# ============================================================
# VidGen - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   copy .env.example .env
# ============================================================

# --- Google Cloud Authentication ---
# Path to your Google Cloud service account JSON key file
# This is used for TTS, STT, and Vertex AI Imagen
GOOGLE_APPLICATION_CREDENTIALS=credentials/cyberize-vertex-api.json

# --- Google Gemini API Key ---
# Used for script summarization, image prompting, and metadata generation
GOOGLE_API_KEY=your-gemini-api-key-here

# --- Google Cloud Project ---
# Your GCP project ID (used for Vertex AI image generation)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_REGION=us-central1

# --- Google Cloud Speech-to-Text ---
# GCS bucket name for uploading large audio files during transcription
# Only needed if you use the YouTube transcription feature
GOOGLE_STT_BUCKET=your-gcs-bucket-name

# --- Speech-to-Text Language (optional) ---
# Default: en-US
# GOOGLE_STT_LANG=en-US
