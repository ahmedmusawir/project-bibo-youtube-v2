# Session Log: 2026-02-24

## Project Context
- **Project:** VidGen (Bibo YouTube Video Generator)
- **Workspace:** /home/moose/python/project-bibo-youtube-v2
- **Primary Goal Today:** Maintain & extend VidGen documentation/playbooks for AI Software Factory agents; keep rolling session notes after each change.

## Starting State Snapshot
- Full documentation suite now lives under `/docs/MAIN DOCS/` plus auxiliary references (`architecture.md`, `decisions.md`, `patterns.md`, etc.).
- Root `README.md` serves as doc index and links into `/docs/`.
- Previous session (2026-02-17) focused on Streamlit pipeline polish and Windows deployment packaging.

## Resources Reviewed This Session
- `docs/MAIN DOCS/*.md` — refreshed on architecture, pipeline modules, AI integrations, Streamlit UI, configuration, project data model, deployment, and alternate routes.
- `docs/architecture.md`, `docs/decisions.md` — captured historical context and key design choices.
- Latest session log (`session-2026-02-17.md`) — baseline for current work.

## Session Activity

### [18:49] — Missing Package Errors After No Code Changes

**User reported:** App worked yesterday, no updates made, but today all pages show `ModuleNotFoundError` for `langchain_google_genai` and `ImportError` for `google.cloud.texttospeech`.

**Diagnosis:**
- User claimed to be running from activated venv, but `$VIRTUAL_ENV` was empty
- `run_app.sh` was missing venv activation — it called `streamlit` from global pyenv Python
- Venv packages verified present: `langchain-google-genai==2.1.2`, `google-cloud-speech==2.36.0`, `google-cloud-texttospeech==2.34.0`
- Global Python was missing these packages (only had `langchain-anthropic`, `langchain-openai`, etc.)

**Root cause:** Two-part issue:
1. `run_app.sh` never activated the venv before launching Streamlit
2. Even with venv activation, pyenv shims intercept the `streamlit` command and use global Python
3. The venv was incomplete — missing `streamlit` and many other packages from `requirements.txt`

**Fix applied:**
1. Installed all packages into venv: `.venv/bin/python -m pip install -r requirements.txt`
2. Updated `run_app.sh` to use `.venv/bin/python -m streamlit run app/main.py` instead of `streamlit run app/main.py`
   - This bypasses pyenv shims entirely and uses the venv's Python directly

**Lesson learned:** 
- When using pyenv, `source .venv/bin/activate` doesn't guarantee the venv's binaries are used — pyenv shims take precedence
- Always use `.venv/bin/python -m <module>` pattern for launcher scripts in pyenv environments
- The venv was never properly initialized with `pip install -r requirements.txt` — it only had manually installed packages

### [19:41] — langchain_classic Import Error

**User reported:** `ModuleNotFoundError: No module named 'langchain_classic'` in `src/summarization.py`

**Root cause:** `langchain_classic` doesn't exist as a package. The correct import is from `langchain` (the main package).

**Fix applied to `src/summarization.py`:**
- Changed `from langchain_classic.chains.combine_documents import create_stuff_documents_chain`
- To: `from langchain.chains.combine_documents import create_stuff_documents_chain`

**Note:** This was likely a typo or outdated reference in the code. The `langchain` package (version 0.3.22) contains the `create_stuff_documents_chain` function.

### [19:52] — New Feature: YouTube Thumbnail Generation

**User request:** Add AI-powered YouTube thumbnail generation to the Metadata page.

**Requirements:**
- Use metadata (titles + description) to create thumbnail-specific image prompt
- Generate catchy one-liner text overlay (3-7 words, ALL CAPS)
- Generate 16:9 thumbnail image via Vertex AI Imagen
- Display thumbnail preview in UI
- Provide download button for PNG file
- Reuse existing image generation infrastructure

**Implementation:**

**New module: `src/thumbnail_generation.py`**
- `generate_thumbnail_text(metadata)` → Uses Gemini LLM to create punchy 3-7 word text overlay
  - Temperature: 0.9 for creative output
  - Prompt engineered for click-worthy power words (REVEALED, SECRET, ULTIMATE, etc.)
- `generate_thumbnail_prompt(metadata, thumbnail_text)` → Creates detailed image prompt
  - Optimized for 16:9 composition with negative space for text overlay
  - Emphasizes high contrast, bold colors, photorealistic/cinematic style
- `create_thumbnail(metadata_path, output_path)` → Orchestrates full pipeline
  - Generates text → Generates prompt → Calls Vertex AI Imagen → Saves PNG + metadata JSON

**UI integration: `app/pages/4_metadata.py`**
- New section after hashtags: "🎨 YouTube Thumbnail"
- **Before generation:** "Generate Thumbnail" button
- **After generation:**
  - Full-width thumbnail preview
  - Shows generated text overlay
  - Expandable image prompt viewer
  - Download button (saves as `{project_name}_thumbnail.png`)
  - Regenerate button
- Real-time logging via `capture_stdout_to_streamlit`
- Lazy import of thumbnail module (only loads when button clicked)

**File outputs:**
- `projects/{name}/4_thumbnail.png` — Generated thumbnail image
- `projects/{name}/4_thumbnail_metadata.json` — Contains thumbnail_text, image_prompt, thumbnail_path

**Design decisions:**
- Thumbnail generation is **optional** — not part of approval flow
- Uses same Imagen model as video images (configurable via `config.json`)
- **Text is embedded directly by Imagen** — The image prompt explicitly instructs the model to render the text on the image with specified placement, style, and contrast
- Higher LLM temperature (0.9) for thumbnail text to encourage creative, punchy output
- Separate from video image generation — different prompt strategy optimized for thumbnails with text rendering

**Correction [20:00]:**
- Initial implementation had text generation separate from image rendering
- Updated to instruct Imagen to embed text directly on the image via the prompt
- Prompt now includes explicit text rendering instructions: placement, style, color, outline

---

---

### [09:20, Feb 26] — YouTube Transcription Format Error Fix

**User issue:** YouTube transcription failing with error: "Requested format is not available"

**Root cause:** 
- yt-dlp's generic `'format': 'bestaudio/best'` not working with current YouTube formats
- YouTube has changed available audio formats, needs more specific format selection

**Fix applied to `src/transcription.py`:**
- Initial attempt: Changed format to `'bestaudio[ext=m4a]/bestaudio[ext=webm]/bestaudio/best'`
- Still failed with "Requested format is not available"
- Upgraded yt-dlp to 2026.2.21 (latest) — still failed
- **Root cause:** Some videos have restricted format availability, postprocessor fails
- **Final solution [09:34]:**
  - Changed format to `'bestaudio*'` (accept any audio format)
  - Use `extract_info()` instead of relying on postprocessor
  - Download whatever format is available
  - Manually convert to mp3 using pydub if needed
  - This bypasses yt-dlp's format selection issues

**Effect:** Works with any YouTube video regardless of available formats

---

### [12:24, Feb 26] — AI Model Selection Dropdowns Feature

**User request:** Add AI model selection dropdowns to multiple pages, matching the voice selection pattern from the Audio page.

**Requirements:**
1. Script/Summarization page — LLM model selector
2. Metadata page — LLM model selector (for titles, descriptions, hashtags, thumbnails)
3. Images page — Two selectors:
   - Image prompting LLM model
   - Image generation model (Imagen variants)
4. Position dropdowns at top of pages under section titles
5. Use same pattern as voice selector from Audio page

**Implementation:**

**1. Script Page (`app/pages/2_script.py`):**
- Added imports: `get_summarization_llm`, `get_available_summarization_llms`, `set_summarization_llm`
- Added "🤖 AI Model Configuration" section at top
- Dropdown shows available summarization models from `config.json`
- Save button appears when selection differs from current
- Models available: gemini-3-flash-preview, gemini-3-pro-preview, gemini-2.5-pro, gemini-2.5-flash

**2. Metadata Page (`app/pages/4_metadata.py`):**
- Added imports: `get_prompting_llm`, `get_available_prompting_llms`, `set_prompting_llm`
- Added "🤖 AI Model Configuration" section at top
- Note: "Used for generating titles, descriptions, hashtags, and thumbnails"
- Single dropdown controls all metadata generation (including thumbnails)
- Models available: gemini-3-flash-preview, gemini-2.5-pro, gemini-2.5-flash

**3. Images Page (`app/pages/5_images.py`):**
- Added imports for both prompting LLM and image generation models
- Added "🤖 AI Model Configuration" section with TWO subsections:
  - **Image Prompt Generation Model** — LLM for creating image prompts
  - **Image Generation Model** — Imagen model for creating images
- Each has its own dropdown and save button
- Image gen models available: imagen-4.0-ultra-generate-001, imagen-4.0-generate-001, imagen-4.0-fast-generate-001

**Pattern Used (matching Audio page):**
```python
# Get available models and current selection
available_models = get_available_xxx_models()
current_model = get_xxx_model()

# Create dropdown with current selection
selected_model = st.selectbox("Select Model", available_models, index=current_index)

# Show info and save button
col1, col2 = st.columns([3, 1])
with col1:
    st.info(f"**Selected:** {selected_model}")
with col2:
    if selected_model != current_model:
        if st.button("💾 Save Model"):
            set_xxx_model(selected_model)
            st.success("✅ Model saved to config")
            st.rerun()
```

**Config System:**
- All models defined in `config/config.json`
- Helper functions in `src/utils/config.py`
- Changes persist across sessions
- No breaking changes — existing code continues to work

**Effect:** Users can now easily switch between AI models for different tasks without editing config files manually. All model selections are saved to `config.json` and used immediately by the respective modules.

---

### [1:10pm, Feb 26] — Individual Regenerate Buttons for Metadata

**User request:** Add ability to regenerate titles or descriptions individually (like the thumbnail regenerate button), not just regenerate all metadata at once.

**Implementation:**

**1. New Functions in `src/metadata_generation.py`:**
- `regenerate_titles(summary_path, metadata_path)` — Regenerates only the 5 titles
- `regenerate_description(summary_path, metadata_path)` — Regenerates only the description
- Both functions load existing metadata, update only their section, and save back to JSON
- Both use the same LLM model from config (prompting_llm)

**2. Updated UI in `app/pages/4_metadata.py`:**
- Added 🔄 button next to "Title Options" header
- Added 🔄 button next to "Description" header
- Each button triggers only its specific regeneration function
- Buttons show tooltips on hover
- Success messages indicate what was regenerated
- Approval status automatically revoked when regenerating

**3. Existing Features Preserved:**
- Full "🔄 Regenerate" button at bottom still regenerates ALL metadata (titles, description, hashtags)
- Thumbnail has its own separate regenerate button (unchanged)
- Manual description editing still available via expander

**UI Pattern:**
```python
col_title, col_regen = st.columns([5, 1])
with col_title:
    st.markdown("### 📌 Title Options")
with col_regen:
    if st.button("🔄", key="regen_titles_btn", help="Regenerate titles"):
        # Regenerate only titles
```

**Effect:** Users can now fine-tune their metadata by regenerating individual sections without losing work on other sections. More granular control over content generation.

---

### [2:45pm, Feb 27] — Auto-Select Newly Created Projects

**User issue:** When creating a new project, it doesn't get automatically selected. User has to manually find and click it in the project list.

**Root cause:** The selectbox widget wasn't properly refreshing after project creation, even though `st.session_state.current_project` was being set correctly.

**Fix in `app/components/sidebar.py`:**
1. Added explicit widget key clearing before rerun (lines 75-76)
2. Improved project selection logic to avoid unnecessary resets (lines 22-29)
3. Added error handling for index lookup (lines 32-37)

**Changes:**
```python
# Clear the selector widget to force refresh
if "project_selector" in st.session_state:
    del st.session_state["project_selector"]
st.rerun()
```

**Effect:** Newly created projects are now automatically selected and loaded, eliminating the need to manually find and select them from the dropdown.

**Note:** This fix was reverted as it didn't work as expected. Issue remains open for future investigation.

---

### [3:03pm, Feb 27] — Image Model Selector for Thumbnail Generation

**User issue:** Thumbnail text embedding is getting messed up. User wants to use Imagen Ultra specifically since it does a better job with text overlays, but couldn't see which model was being used or change it.

**Current behavior:** Thumbnail generation uses `get_image_gen_model()` from config, but there was no UI control on the Metadata page to select the model specifically for thumbnails.

**Implementation in `app/pages/4_metadata.py`:**
- Added image model selector in the thumbnail section (lines 241-265)
- Positioned right below "YouTube Thumbnail" header
- Shows current model from config
- Dropdown with all available Imagen models (Ultra, Standard, Fast)
- Save button appears when selection differs from current
- Saves to global config (affects all image generation)

**Available Models:**
- `imagen-4.0-ultra-generate-001` — Best quality, best text handling
- `imagen-4.0-generate-001` — Standard quality
- `imagen-4.0-fast-generate-001` — Faster generation

**How it works:**
```python
# Image Model Selector for Thumbnails
st.markdown("**Image Generation Model:**")
selected_image_model = st.selectbox(...)
if selected_image_model != current_image_model:
    if st.button("💾 Save"):
        set_image_gen_model(selected_image_model)
```

**Effect:** Users can now select Imagen Ultra before generating thumbnails to get better text embedding quality. The model selection is visible and controllable directly on the Metadata page where thumbnails are generated.

---

## Notes / Next Steps
- Continue updating this file after each change, decision, or lesson learned.
- User should test thumbnail generation on a project with existing metadata.
- All model selection features ready to use.
